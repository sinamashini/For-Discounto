import { css, Global } from "@emotion/react"

export const globalStyles = (
  <Global
    styles={css`
      html,
      body {
        font-family: IRANYekan, 'IRANYekan(FaNum)';
      }
    `}
  />
)
